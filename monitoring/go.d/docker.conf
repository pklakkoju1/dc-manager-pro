# ── Docker collector ───────────────────────────────────────────
# Monitors container state, health checks, and restarts.
# Alerts fire when a container is unhealthy or restarting.
jobs:
  - name: local
    address: unix:///var/run/docker.sock
