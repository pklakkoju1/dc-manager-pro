# ── Nginx collector for dcm_frontend ──────────────────────────
# Requires nginx stub_status module — enabled in our nginx.conf
# Metrics: active connections, requests/sec, handled, reading, writing, waiting
jobs:
  - name: dcm_frontend
    url: http://localhost/nginx_status
